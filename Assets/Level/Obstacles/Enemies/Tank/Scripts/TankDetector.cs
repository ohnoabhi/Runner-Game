using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TankDetector : MonoBehaviour
{
    private void OnCollisionEnter(Collision other)
    {
        // if (Tank.Get(other, out var tank))
        // {
        //     tank.Die();
        // }
    }
}